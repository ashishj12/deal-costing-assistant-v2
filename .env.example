# AI provider selection. Mock is the default and requires no credentials.
# Valid values: mock | gemini
AI_PROVIDER=mock

# Server-side only. Never expose this to the browser bundle.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# Generation guardrails
AI_TIMEOUT_MS=30000
AI_MAX_RETRIES=2
